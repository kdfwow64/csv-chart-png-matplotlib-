when you have them cloned, you'll want to `yarn && yarn run start:devDocker` on `aime-api`.... then `yarn && yarn run start:frontend-dev-demo` on `aime-eval`


When they are successfully running you'll have several Docker containers for the API servers (including Janus):
